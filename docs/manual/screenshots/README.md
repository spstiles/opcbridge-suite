# Screenshots (Placeholders)

This directory is reserved for manual screenshots.

Recommended conventions:

- PNG preferred for UI screenshots.
- Filenames are kebab-case.
- Keep images cropped to the relevant panel.
- Avoid including secrets/tokens in screenshots.

## Needed Screenshots

1. SCADA landing/status panel
2. SCADA Configure Server tab (versions + URLs)
3. SCADA Workspace tree + connection editor
4. SCADA Workspace tag editor (Modbus example + word order)
5. Alarms & Events tab (routes + schedules)
6. Phone/SIP notification setup (policy + targets)
7. Audio test + TTS test panels
8. Data Logger tab (databases + logger job)
9. Historian tab (health/config)
10. HMI runtime view + editor toggle

